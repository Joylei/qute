use crate::ctx::Context;
use anyhow::Result;
use pico_args::Arguments;

#[allow(unused)]
pub fn run(_args: &mut Arguments, _ctx: &Context) -> Result<()> {
    println!("To Be Done!");
    Ok(())
}
